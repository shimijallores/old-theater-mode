# Old Theater Mode

I had to make this cuz im annoyed by the new theater mode

## Installation

### Google Chrome

1. Open Google Chrome and navigate to `chrome://extensions/`.
2. Enable "Developer mode" by toggling the switch in the top right corner.
3. Click on "Load unpacked" and select the folder containing this extension (the folder with `manifest.json`).

### Firefox

1. Open Firefox and navigate to `about:debugging`.
2. Click on "This Firefox" in the left sidebar.
3. Click on "Load Temporary Add-on" and select the `manifest.json` file from this extension's folder.

you can install it yourself mwehhee i dont have money for webstore fee

### Example

![Extension demo](/screenshots/demo.png)
